# Docker环境变量配置模板
# 复制此文件为 .env.docker 并填写实际值
# 使用方法：cp env.docker.template .env.docker

# Temu API配置（必须填写）
TEMU_APP_KEY=your_temu_app_key_here
TEMU_APP_SECRET=your_temu_app_secret_here

# 数据库配置（Docker已自动配置，一般无需修改）
POSTGRES_DB=temu_omni
POSTGRES_USER=temu_user
POSTGRES_PASSWORD=temu_password

# 应用配置
DEBUG=True
SECRET_KEY=dev-secret-key-for-docker-development

